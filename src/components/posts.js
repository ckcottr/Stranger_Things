import React from "react";
import ReactDOM from "react-dom"

const Posts = ({post, setPost}) => {
    use
    return (
        <div>
            <h1>POSTS</h1>
        </div>
    )
}

export default Posts